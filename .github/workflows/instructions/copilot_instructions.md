# Portfolio Website Rules

## Header Consistency (Strict)
- The Home page header is the single source of truth.
- All pages MUST reuse the exact same header design.
- Do NOT create new header variants.
- Reuse the existing header component or markup.
- If a page needs a header, import or reference the Home page header instead of redesigning it.
